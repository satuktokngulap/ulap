On RDP VM:
1. Copy this folder to /opt/

2. Add this line to /etc/rc.local
   python /opt/lb_rdp/lb_rdp.py
   
<UPSTART>   
3. Copy the upstart script file 'upstart/lb_rdp.conf' inside '/etc/init/'

4. Issue command 'initctl list' and see if lb_rdp is on the list
    If status is 'stop/wating' issue command 'start lb_rdp'
    Issue command 'status lb_rdp' and the process 'lb_rdp' should be running

